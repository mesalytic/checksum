import chalk from 'chalk';

export async function verifyChecksum(_filePath: string, _checksum: string, _algorithm: string) {
    // TODO: verify feature
    console.log(`${chalk.yellow('Verify functionality is not yet implemented.')}`);
}