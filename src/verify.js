import chalk from 'chalk';

export async function verifyChecksum(filePath, checksum, algorithm) {
    // TODO: verify feature
    console.log(`${chalk.yellow('Verify functionality is not yet implemented.')}`);
}